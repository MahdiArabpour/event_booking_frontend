import 'package:flutter/material.dart';

class BookedEventsTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('BookedEvents'),
    );
  }
}
